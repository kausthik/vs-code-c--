#include<iostream>
using namespace std;
// minimum platform
int main(){
        vector<int> v;
    int n,p,sum=0;
    cout<<"enter the number of elements in array ";
    cin>>n;
    for(int i=0;i<n;i++){
       int x;
       cin>>x;
       v.push_back(x);
    }
    // find the number of platform
}